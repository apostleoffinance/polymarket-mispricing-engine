DATABASE_URL=postgres://localhost:5433/polymarket

# Optional: LLM relationship hypotheses (candidates only; stats still validate).
# Fallback order is configured in research_engine/config.py (openai → gemini).
OPENAI_API_KEY=
GEMINI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
